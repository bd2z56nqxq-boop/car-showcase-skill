node_modules
dist
out
.DS_Store
.env

# Don't commit 3D model files — users supply their own
public/models/*.fbx
public/models/*.glb
public/models/*.gltf

# But keep the README
!public/models/README.md
